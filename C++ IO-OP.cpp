#include<iostream>
using namespace std;
int main()
{
	int x=15;      //created a variable x
	
	cin>>x;       //  for user input we does not need the value of int x in program

	
	cout<<"user input="<<x;/*in 'a' ke jagha hum dusra bhi likh sakhte hai.
	                ex. x is variable it store the data is 10 and we want 
					the put the value of x in cout then we added <<x to 
					show the output in cout*/
	
                    //print the value of variable x
					
	return 0;
}
